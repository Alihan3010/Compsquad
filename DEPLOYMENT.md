# 🚀 Развертывание сайта на облачных сервисах

## 📌 Быстрые варианты (самые простые)

### 1. GitHub Pages (САМЫЙ ПРОСТОЙ)
**Время:** 5 минут | **Стоимость:** БЕСПЛАТНО | **Сложность:** ⭐

#### Шаги:
1. Создайте аккаунт на https://github.com
2. Создайте новый репозиторий с именем `tourism-vko`
3. Загрузьте файлы:
   - index.html
   - styles.css
   - script.js
4. Перейдите в Settings → Pages
5. Выберите Branch: main
6. Нажмите Save
7. ✅ Готово! Сайт будет доступен по адресу: `https://yourusername.github.io/tourism-vko/`

**⚠️ Важно:** Прямой доступ к OpenAI API из GitHub Pages может быть заблокирован CORS. Используйте серверную часть для полной функциональности.

---

### 2. Netlify (РЕКОМЕНДУЕТСЯ)
**Время:** 5 минут | **Стоимость:** БЕСПЛАТНО | **Сложность:** ⭐

#### Шаги:
1. Перейдите на https://netlify.com
2. Нажмите "Sign up" (зарегистрируйтесь)
3. Выберите GitHub для входа
4. Авторизуйте Netlify
5. Нажмите "New site from Git"
6. Выберите репозиторий `tourism-vko`
7. Netlify автоматически развернет сайт
8. ✅ Готово! Сайт доступен по адресу типа: `your-site.netlify.app`

**Преимущества:**
- Автоматическое обновление при push в GitHub
- Free SSL сертификат
- CDN во всем мире
- Поддержка функций (для серверной части)

---

### 3. Vercel (АЛЬТЕРНАТИВА)
**Время:** 5 минут | **Стоимость:** БЕСПЛАТНО | **Сложность:** ⭐

#### Шаги:
1. Перейдите на https://vercel.com
2. Нажмите "Sign Up" с GitHub
3. Авторизуйте Vercel
4. Нажмите "New Project"
5. Импортируйте репозиторий GitHub
6. Нажмите Deploy
7. ✅ Готово! Сайт доступен по адресу типа: `your-project.vercel.app`

**Преимущества:**
- Очень быстрый хостинг
- Оптимизация изображений
- Поддержка API Routes
- Бесплатные домены Vercel

---

## 🔧 Вариант с серверной частью

### Node.js + Heroku
**Время:** 15 минут | **Стоимость:** БЕСПЛАТНО (нужна карта) | **Сложность:** ⭐⭐

#### Файлы для Heroku:

**Procfile:**
```
web: node server.js
```

**package.json:**
```json
{
  "name": "tourism-vko",
  "version": "1.0.0",
  "description": "Tourism in Eastern Kazakhstan",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "axios": "^1.3.0"
  },
  "engines": {
    "node": "16.x"
  }
}
```

#### Развертывание:
1. Установите Heroku CLI: https://devcenter.heroku.com/articles/heroku-cli
2. Создайте аккаунт на https://heroku.com
3. В папке проекта выполните:
```bash
heroku login
heroku create your-app-name
heroku config:set OPENAI_API_KEY=sk-xxxxx
git push heroku main
```
4. ✅ Готово! Сайт доступен по адресу: `https://your-app-name.herokuapp.com`

---

### Python Flask + Railway
**Время:** 10 минут | **Стоимость:** БЕСПЛАТНО | **Сложность:** ⭐⭐

#### Fichier app.py (вместо server.js):
```python
from flask import Flask, render_template, request, jsonify
from flask_cors import CORS
import openai
import os

app = Flask(__name__)
CORS(app)

openai.api_key = os.getenv('OPENAI_API_KEY')

@app.route('/')
def index():
    # Верните HTML файл
    return render_template('index.html')

@app.route('/api/search', methods=['POST'])
def search():
    data = request.get_json()
    query = data.get('query')

    try:
        response = openai.ChatCompletion.create(
            model='gpt-3.5-turbo',
            messages=[
                {'role': 'system', 'content': 'Ты эксперт по туризму в ВКО'},
                {'role': 'user', 'content': query}
            ]
        )
        return jsonify({'success': True, 'result': response['choices'][0]['message']['content']})
    except Exception as e:
        return jsonify({'error': str(e)}), 500

if __name__ == '__main__':
    app.run(debug=False, port=int(os.getenv('PORT', 5000)))
```

#### Развертывание на Railway:
1. Перейдите на https://railway.app
2. Нажмите "Start New Project"
3. Выберите "GitHub Repo"
4. Авторизуйте GitHub
5. Выберите ваш репозиторий
6. Railway автоматически обнаружит Python
7. Добавьте переменные окружения:
   - `OPENAI_API_KEY = sk-xxxxx`
   - `FLASK_ENV = production`
8. ✅ Готово! Сайт развернут

---

## 🌐 Собственный домен

### Подключение кастомного домена к Netlify:
1. Купите домен (например, на Namecheap или Google Domains)
2. В настройках Netlify: Custom domain
3. Введите ваш домен
4. Следуйте инструкциям для DNS
5. ✅ Готово! Доступен по `https://yourdomain.com`

### Подключение к Vercel:
1. В Vercel Settings → Domains
2. Добавьте ваш домен
3. Обновите DNS записи на регистраторе
4. ✅ Готово!

---

## 📋 Чек-лист перед развертыванием

### Безопасность:
- ✅ API ключи в переменных окружения (.env), не в коде
- ✅ HTTPS сертификат (автоматически на всех популярных хостингах)
- ✅ CORS настроен правильно
- ✅ Нет чувствительных данных в коде

### Функциональность:
- ✅ Протестирован поиск локально
- ✅ Карта загружается корректно
- ✅ История поисков работает
- ✅ Модальные окна открываются/закрываются

### А производительности:
- ✅ Страница загружается менее 3 секунд
- ✅ Нет ошибок в консоли (F12)
- ✅ Мобильная версия работает
- ✅ CSS и JS минифицированы (опционально)

### SEO (для видимости в поиске):
- ✅ Meta тегов в index.html
- ✅ Правильный title и description
- ✅ Структурированные данные (schema.org)

---

## 🔄 CI/CD Автоматизация

### GitHub Actions (автоматический деплой)

Crear `.github/workflows/deploy.yml`:
```yaml
name: Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    
    - name: Deploy to Netlify
      uses: nwtgck/actions-netlify@v1.2
      with:
        publish-dir: '.'
        production-branch: main
        github-token: ${{ secrets.GITHUB_TOKEN }}
        deploy-message: ${{ github.event.head_commit.message }}
      env:
        NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
        NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
```

---

## 📊 Сравнение платформ

```
┌──────────────┬──────┬──────┬──────────┬─────────────────┐
│ Платформа    │ Цена │ Тип  │ CORS     │ Сложность       │
├──────────────┼──────┼──────┼──────────┼─────────────────┤
│ GitHub Pages │ Бес. │ Стат │ Проблемы │ ⭐ (очень просто) │
│ Netlify      │ Бес. │ Стат │ OK       │ ⭐ (очень просто) │
│ Vercel       │ Бес. │ Стат │ OK       │ ⭐ (очень просто) │
│ Heroku       │ Бес. │ Дин. │ OK       │ ⭐⭐ (просто)      │
│ Railway      │ Бес. │ Дин. │ OK       │ ⭐⭐ (просто)      │
│ AWS          │ Плат │ Оба  │ OK       │ ⭐⭐⭐ (сложно)     │
│ DigitalOcean │ Плат │ Оба  │ OK       │ ⭐⭐⭐ (сложно)     │
└──────────────┴──────┴──────┴──────────┴─────────────────┘

Статистическое (Static) - только HTML/CSS/JS
Динамическое (Dynamic) - с серверной частью
```

---

## 🎯 Мой рекомендуемый путь:

### Для быстрого старта (1 час):
1. GitHub Pages (самый простой)
2. Используйте демо режим полностью
3. Работает без API ключей

### Для полной функциональности (3 часа):
1. Получите OpenAI API ключ
2. Получите 2GIS API ключ
3. Вставьте в script.js
4. Развертните на Netlify
5. На Netlify есть проблемы с CORS → обходим их

### Для продакшена (день работы):
1. Создайте Node.js сервер (server.js уже готов)
2. Разверните на Railway или Heroku
3. Подключите кастомный домен
4. Настройте мониторинг ошибок

---

## 🐛 Типичные ошибки и решения

### "CORS error" при поиске
**Причина:** Браузер блокирует прямые запросы к OpenAI API  
**Решение:** Используйте серверную часть (Node.js/Flask)

### "2GIS карта не загружается"
**Причина:** Неправильный API ключ или CORS блокировка  
**Решение:** Проверьте ключ в скрипте, получите новый ключ

### "Сайт загружается долго"
**Причина:** Большие файлы или медленное соединение  
**Решение:** Оптимизируйте изображения, используйте CDN (автоматически на всех хостингах)

### "Мобильная версия не работает"
**Причина:** CSS не загрузился корректно  
**Решение:** Проверьте пути к файлам в index.html

---

## 📞 Ссылки на сервисы

- **GitHub Pages:** https://pages.github.com/
- **Netlify:** https://netlify.com
- **Vercel:** https://vercel.com
- **Heroku:** https://heroku.com (в 2023 убрали free plan)
- **Railway:** https://railway.app
- **Render:** https://render.com
- **AWS:** https://aws.amazon.com
- **DigitalOcean:** https://digitalocean.com

---

**Рекомендация:** Начните с Netlify - это самое простое и бесплатное решение! ⭐

---
Дата создания: 13 февраля 2026  
Версия: 1.0
